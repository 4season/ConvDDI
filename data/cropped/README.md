# 📁 data/cropped/

> ⚠️ **이 디렉터리에는 약품 크롭 이미지 데이터(약 43,350장)가 포함되어야 하지만, 데이터 보호 정책상 공개할 수 없습니다.**

## 데이터 설명

| 항목 | 내용 |
|---|---|
| 내용 | bbox 크롭된 단일 약품 이미지 |
| 총 수량 | 약 43,350장 |
| 해상도 | 128 × 128 (리사이즈 후) |
| 클래스 수 | 100종 |
| 폴더 구조 | `{idx:03d}_{drug_code}/` (예: `000_K-031863/`) |

## 재현 방법

원본 데이터를 `data/images/` 및 `data/labels/`에 배치한 뒤 아래 명령으로 크롭 이미지를 생성할 수 있습니다:

```bash
python src/main.py preprocess
```
